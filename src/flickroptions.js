export default {
  // optional - preload primary (large) images before attempting to display them
  PRELOAD_IMAGES: true,

  // optional - e.g. "random", "reverse", "default"
  // random will also affect the album ordering
  ORDERING: "reverse"
};
