export default {
  // optional - preload primary (large) images before attempting to display them
  PRELOAD_IMAGES: true,

  // optional - randomise display order of photos and albums
  RANDOMISE: false
};
